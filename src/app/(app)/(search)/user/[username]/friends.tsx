// Thin route re-export — see src/components/ProfileFriendsListScreen.tsx.
// Duplicated (unchanged) under (profile)/user/[username]/friends.tsx so
// this list stays in whichever tab PublicProfileScreen was opened from.
export { ProfileFriendsListScreen as default } from '@/components/ProfileFriendsListScreen';
