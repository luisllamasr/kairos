# --- Secrets and local configuration ---
# Never commit credentials, API keys, or environment-specific values.
.env
.env.*
!.env.example
docs/CREDENTIALS.md

# --- Dependencies ---
node_modules/

# --- Expo / React Native ---
.expo/
dist/
web-build/
expo-env.d.ts
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*

# Metro / bundler caches
.metro-health-check*

# Native build artifacts (prebuild / bare workflow)
ios/
android/
.kotlin/

# --- Supabase local tooling ---
.branches
.temp
supabase/.branches
supabase/.temp
.tools/

# --- Logs and debug ---
npm-debug.*
yarn-debug.*
yarn-error.*
*.log

# --- OS files ---
.DS_Store
Thumbs.db

# --- Editor / IDE (local only) ---
.idea/
# Ignore personal VS Code preferences but commit project-wide settings
# (e.g. Deno enablePaths) so every developer gets the correct configuration.
.vscode/*
!.vscode/settings.json
*.swp
*.swo
*~

# --- TypeScript ---
*.tsbuildinfo

# --- Test / coverage ---
coverage/
